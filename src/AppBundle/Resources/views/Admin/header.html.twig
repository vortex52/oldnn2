<aside class="col-12 col-sm-2 p-0 bg-dark">
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark align-items-start flex-sm-column flex-row">

        <a class="navbar-brand" href="{{ path('home') }}"><i class="fa fa-home"></i> Home</a>

        <a href class="navbar-toggler" data-toggle="collapse" data-target=".sidebar">
           <span class="navbar-toggler-icon"></span>
        </a>

        <div class="collapse navbar-collapse sidebar">
            <ul class="flex-column navbar-nav w-100 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link pl-0 dropdown-toggle text-nowrap" href="#m0" data-toggle="collapse" data-target="#m0" aria-expanded="false">
                        <i class="fa fa-user-secret"></i> <span class="">{{ app.user.username }}</span>
                    </a>
                    <div class="collapse" id="m0">
                        <ul class="flex-column nav">
                            <a class="nav-link px-0 text-truncate" href="{{ path('logout') }}">{{ "exit"|trans() }}</a>
                        </ul>
                    </div>
                </li>

                <li class="nav-item">
                    <a class="nav-link pl-0 dropdown-toggle text-nowrap" href="#m1" data-toggle="collapse" data-target="#m1" aria-expanded="false">
                        <i class="fa fa-cogs"></i> <span class=""> {{ "setting"|trans() }}</span>
                    </a>
                    <div class="collapse" id="m1">
                        <ul class="flex-column nav">
                            <a class="nav-link px-0 text-truncate" href="{{ path('admin.get.users') }}">{{ "admin.nav.users"|trans() }}</a>
                            <a class="nav-link px-0 text-truncate" href="{{ path('admin.get.points') }}">
                            {{ "admin.nav.list"|trans() }}</a>
                            <a class="nav-link px-0 text-truncate" href="{{ path('user.new.point') }}">{{ "admin.nav.add_mark"|trans() }}</a>
                        </ul>
                    </div>
                </li>

                <li class="nav-item">
                    <a class="nav-link pl-0" href="{{ path('admin.clear_cache') }}"><i class="fa fa-eraser"></i> <span class="">Cache clear</span></a>
                </li>                
            </ul>
        </div>
    </nav>
</aside>
