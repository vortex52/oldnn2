admin.index:
    path: /index
    defaults:
        _controller: AppBundle:Admin/Admin:index

admin.save.points.file:
    path: /save_points
    defaults:
        _controller: AppBundle:Admin/Admin:savePointsFile

admin.clear_cache:
    path: /clear_cache
    defaults:
        _controller: AppBundle:Admin/Command:сlearCache

admin.get.points:
    path: /get_points
    defaults:
        _controller: AppBundle:Admin/Admin:getPoints

admin.edit.point:
    path: /{id}/edit_point
    defaults:
        _controller: AppBundle:Admin/Admin:editPoint

admin.del.point:
    path: /{id}/del_point
    defaults:
        _controller: AppBundle:Admin/Admin:deletePoint

admin.get.users:
    path: /get_users
    defaults:
        _controller: AppBundle:Admin/Admin:getUsers

admin.edit.user:
    path: /{id}/edit_user
    defaults:
        _controller: AppBundle:Admin/Admin:editUser
        
admin.del.user:
    path: /{id}/del_user
    defaults:
        _controller: AppBundle:Admin/Admin:deleteUser